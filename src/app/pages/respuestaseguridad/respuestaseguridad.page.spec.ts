import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RespuestaseguridadPage } from './respuestaseguridad.page';

describe('RespuestaseguridadPage', () => {
  let component: RespuestaseguridadPage;
  let fixture: ComponentFixture<RespuestaseguridadPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RespuestaseguridadPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
